# standard install requirements
-r ./requirements.txt

scipy >= 1.10.0, <= 1.17

# building the docs
sphinx >= 3.0.0, < 9.2
sphinx_copybutton < 0.6
sphinx-rtd-theme >= 1.0, < 3.2
sphinx-github-style >= 1.0, < 1.3

# linting
# keep this pin in sync with .pre-commit-config.yaml and .github/workflows/ruff.yml
ruff == 0.15.14
pre-commit < 4.7
pyright < 1.2

# testing and benchmarking
pytest >= 5.0.0, < 9.1
pytest-cov >= 2.6, < 7.2
pytest-monitor < 1.7

# building
build
